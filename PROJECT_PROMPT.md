# Промпт проекта: ATOM ДОБРО - Карта добрых дел атомных городов

## 📋 Общее описание проекта

**ATOM ДОБРО** — это веб-приложение для картографирования и управления волонтерскими организациями и квестами в атомных городах России. Платформа позволяет пользователям находить организации, участвовать в квестах, отслеживать свой прогресс и достижения, а также создавать собственные организации и квесты.

### Основная цель
Создание единой платформы для координации волонтерской деятельности, помощи организациям в привлечении волонтеров и ресурсов, а также мотивации пользователей через систему достижений и опыта.

---

## 🛠 Технологический стек

### Frontend Framework & Core
- **React 19.2.0** — основной UI-фреймворк
- **TypeScript 5.9.3** — типизация
- **Vite 7.2.2** — сборщик и dev-сервер
- **React Router DOM 7.9.6** — маршрутизация (используется file-based routing через `w2b-vite-filebased-routing`)

### State Management
- **Redux Toolkit 2.10.1** — управление состоянием
- **RTK Query** — для работы с API (встроен в Redux Toolkit)
- **Redux Persist 6.0.0** — сохранение состояния в localStorage
- **React Redux 9.2.0** — интеграция React с Redux

### UI & Styling
- **Tailwind CSS 4.1.17** — utility-first CSS фреймворк
- **Radix UI** — компоненты (`@radix-ui/react-label`, `@radix-ui/react-slot`)
- **Lucide React 0.553.0** — иконки
- **Sonner 2.0.7** — уведомления (toast)
- **Class Variance Authority** — управление вариантами компонентов

### Forms & Validation
- **React Hook Form 7.66.0** — управление формами
- **Zod 4.1.12** — валидация схем
- **@hookform/resolvers 5.2.2** — интеграция Zod с React Hook Form

### Maps & Geolocation
- **Leaflet 1.9.4** — картографическая библиотека
- **React Leaflet 5.0.0** — React-обертка для Leaflet
- **Leaflet Default Icon Compatibility** — совместимость иконок

### Дополнительные библиотеки
- **Next Themes 0.4.6** — управление темами (темная/светлая)
- **clsx & tailwind-merge** — утилиты для работы с классами CSS

### Development Tools
- **ESLint 9.39.1** — линтер
- **TypeScript ESLint** — правила для TypeScript
- **Vite Plugin React** — поддержка React в Vite

---

## 📁 Структура проекта

```
hakaton2/
├── src/
│   ├── components/          # React компоненты
│   │   ├── forms/          # Формы (логин, регистрация, организация, квест)
│   │   ├── map/            # Компоненты карты
│   │   ├── profile/        # Компоненты профиля
│   │   ├── header/         # Шапка сайта
│   │   ├── notifications/  # Уведомления
│   │   └── ui/             # Базовые UI компоненты
│   ├── store/              # Redux store и RTK Query сервисы
│   │   └── entities/       # API сервисы по сущностям
│   │       ├── auth/       # Аутентификация
│   │       ├── organization/# Организации
│   │       ├── quest/      # Квесты
│   │       ├── achievement/# Достижения
│   │       └── experience/ # Опыт пользователя
│   ├── pages/              # Страницы (file-based routing)
│   │   ├── (auth)/         # Страницы авторизации
│   │   └── (header)/       # Страницы с шапкой
│   ├── hooks/              # Кастомные React хуки
│   ├── contexts/           # React Context (UserContext, NotificationContext)
│   ├── utils/              # Утилиты
│   ├── types/              # TypeScript типы
│   └── styles/             # Глобальные стили
├── public/                 # Статические файлы
├── organizations.json      # Данные организаций (mock/seed)
├── data.md                 # Исходные данные для парсинга
└── package.json
```

---

## 🎯 Основной функционал

### 1. Карта организаций и квестов
- **Интерактивная карта** на базе Leaflet с маркерами организаций и квестов
- **Фильтрация** по:
  - Городу
  - Типу организации
  - Виду помощи (help types)
  - Типу метки (организации/квесты/все)
  - Поиск по названию
- **Детальная информация** об организациях и квестах в модальных окнах
- **Список** организаций и квестов с возможностью перехода к деталям

### 2. Аутентификация и авторизация
- Регистрация нового пользователя
- Вход в систему (email/password)
- Защищенные маршруты (`ProtectedRoute`)
- Управление сессией через токены (JWT)
- Сохранение состояния авторизации в localStorage

### 3. Управление организациями
- **Создание организации** через форму с разделами:
  - Основная информация (название, миссия, описание)
  - Контакты
  - Местоположение (выбор на карте)
  - Виды помощи
  - Цели и потребности
  - Галерея изображений
- **Редактирование организации** (если пользователь является создателем)
- **Удаление организации** (danger zone)
- **Загрузка изображений** через API (`/api/v1/upload/images`)

### 4. Управление квестами
- **Создание квеста** с этапами:
  - Основная информация (название, история, категория)
  - Этапы квеста с требованиями (волонтеры, финансы, вещи)
  - Контакты куратора
  - Социальные сети
  - Галерея
  - Достижения
- **Участие в квесте** с получением опыта
- **Отслеживание прогресса** по этапам

### 5. Профиль пользователя
- **Информация о пользователе**:
  - Аватар (с возможностью загрузки)
  - Имя, email
  - Уровень и опыт
  - Статистика (квесты, достижения, пожертвования, часы волонтерства)
- **Достижения** — список полученных достижений
- **Активные квесты** — квесты, в которых участвует пользователь
- **История вклада** — история участия в квестах

### 6. Система достижений и опыта
- **Уровни пользователя** с прогресс-баром
- **Опыт** за участие в квестах
- **Достижения** с различными редкостями (common, rare, epic, legendary)
- **Уведомления** о получении опыта и достижений

---

## 🔌 API и Backend интеграция

### Base URL
```
http://82.202.140.37:3000/api/v1
```

### Основные эндпоинты

#### Аутентификация (`authService`)
- `POST /auth/register` — регистрация
- `POST /auth/login` — вход
- `GET /users/:userId` — получение данных пользователя
- `PATCH /users/:userId` — обновление пользователя

#### Организации (`organizationService`)
- `GET /organizations` — список организаций
- `GET /organizations/:organizationId` — детали организации
- `POST /organizations` — создание организации
- `PATCH /organizations/:organizationId` — обновление организации
- `DELETE /organizations/:organizationId` — удаление организации
- `GET /organization-types` — типы организаций
- `GET /help-types` — виды помощи
- `GET /cities` — список городов
- `POST /upload/images` — загрузка изображений

#### Квесты (`questService`)
- `GET /quests` — список квестов
- `GET /quests/:questId` — детали квеста
- `POST /quests` — создание квеста
- `PATCH /quests/:questId` — обновление квеста
- `DELETE /quests/:questId` — удаление квеста
- `POST /quests/:questId/participate` — участие в квесте

#### Опыт и достижения
- `POST /experience` — добавление опыта
- `GET /achievements` — список достижений

### Формат данных

#### Пользователь
```typescript
{
  id: string
  firstName: string
  lastName: string
  email: string
  avatar?: string | number
  avatarUrls?: Record<number, string>  // ID -> URL
  level: number
  experience: number
  stats: { ... }
  achievements: Array<{ ... }>
  // ...
}
```

**Важно:** Аватары могут приходить в формате:
- `avatar: number` — ID изображения, URL формируется как `/api/v1/upload/images/{id}`
- `avatarUrls: { 4: 'url', 5: 'url' }` — объект с ID и URL

#### Организация
```typescript
{
  id: string
  name: string
  cityId: number
  typeId: number
  helpTypeIds: number[]
  latitude: string
  longitude: string
  summary: string
  mission: string
  description: string
  goals: string[]
  needs: string[]
  address: string
  contacts: Array<{ name: string, value: string }>
  gallery: string[]  // URLs изображений
  organizationTypes?: Array<{ id: number, name: string }>
  type?: { id: number, name: string }  // Альтернативный формат
}
```

---

## 🎨 UI/UX особенности

### Дизайн
- Современный, чистый интерфейс
- Адаптивный дизайн (mobile-first)
- Использование градиентов и backdrop-blur эффектов
- Анимации и переходы

### Компоненты
- **Button** — различные варианты (primary, outline, ghost)
- **Form** — компоненты форм с валидацией
- **ImageGallery** — галерея изображений с просмотром
- **Spinner** — индикаторы загрузки
- **Skeleton** — скелетоны для загрузки контента

### Карта
- Кастомные иконки маркеров
- Попапы с краткой информацией
- Детальные модальные окна
- Поиск по адресу
- Фильтры и список объектов

---

## 🔐 Безопасность и авторизация

- **JWT токены** для аутентификации
- Токены хранятся в `localStorage`
- Заголовок `Authorization: Bearer {token}` для всех запросов
- Защищенные маршруты через `ProtectedRoute`
- Проверка прав доступа (только создатель может редактировать/удалять)

---

## 📦 Управление состоянием

### Redux Store структура
```typescript
{
  authApi: AuthState,           // RTK Query кеш для auth
  questApi: QuestState,         // RTK Query кеш для quests
  achievementApi: AchievementState,
  experienceApi: ExperienceState,
  organizationApi: OrganizationState
}
```

### React Context
- **UserContext** — текущий пользователь и методы обновления
- **NotificationContext** — уведомления

### Локальное хранилище
- Redux Persist для сохранения состояния авторизации
- localStorage для пользовательских данных (квесты, организации)

---

## 🧪 Особенности реализации

### Обработка изображений
- Сжатие изображений перед загрузкой (`compressImage`)
- Поддержка base64 и URL
- Загрузка через FormData на `/api/v1/upload/images`
- Обработка `avatarUrls` для аватаров пользователей

### Формы
- Валидация через Zod схемы
- React Hook Form для управления состоянием
- Многошаговые формы для квестов
- Автосохранение координат карты

### Фильтрация
- Динамические фильтры на основе данных API
- Комбинирование фильтров (AND логика)
- Поиск по названию с учетом всех полей

### Опыт и уровни
- Система уровней с прогресс-баром
- Начисление опыта за действия
- Уведомления о повышении уровня
- Расчет опыта до следующего уровня

---

## 🚀 Запуск проекта

### Установка зависимостей
```bash
pnpm install
```

### Разработка
```bash
pnpm dev
```

### Сборка
```bash
pnpm build
```

### Превью production сборки
```bash
pnpm preview
```

### Линтинг
```bash
pnpm lint
```

---

## 📝 Важные заметки для разработчиков

### File-based Routing
Проект использует `w2b-vite-filebased-routing` для автоматической генерации маршрутов на основе структуры папок `src/pages/`.

### Типы данных
- Все типы определены в `src/types/`
- API типы в `src/store/entities/*/model/type.ts`
- Используется строгая типизация TypeScript

### Обработка ошибок
- RTK Query автоматически обрабатывает ошибки API
- Toast уведомления для пользователя
- Логирование в dev режиме

### Производительность
- React.memo для оптимизации ре-рендеров
- useMemo и useCallback где необходимо
- Lazy loading для маршрутов
- Кеширование через RTK Query

### Совместимость API
- API может возвращать данные в разных форматах (например, `type` vs `organizationTypes`)
- Реализована нормализация данных для единообразия
- Обратная совместимость с различными версиями API

---

## 🎯 Основные сущности

1. **User** — пользователь системы
2. **Organization** — волонтерская организация
3. **Quest** — квест/задача для волонтеров
4. **Achievement** — достижение пользователя
5. **HelpType** — вид помощи (волонтеры, пожертвования, вещи и т.д.)
6. **OrganizationType** — тип организации (НКО, фонд и т.д.)
7. **City** — город

---

## 📚 Дополнительная информация

- Проект создан для хакатона
- Использует фирменные цвета Росатом
- Поддержка атомных городов России
- Интеграция с социальными сетями (VK, Telegram, WhatsApp)

---

*Последнее обновление: 2025*

