/**
 * @title Сброс пароля
 * @description Установите новый пароль для вашего аккаунта. Используйте ссылку из письма для сброса пароля
 * @keywords сброс пароля, новый пароль, установить пароль, изменить пароль, reset password
 * @changefreq monthly
 * @priority 0.5
 */

import { ResetPasswordForm } from '@/components/forms/reset-password'

export default function ResetPasswordPage() {
	return <ResetPasswordForm />
}

