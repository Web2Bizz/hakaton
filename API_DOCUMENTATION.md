# API Документация для Backend разработчика

## Содержание

1. [Общая информация](#общая-информация)
2. [Аутентификация и авторизация](#аутентификация-и-авторизация)
3. [Пользователи](#пользователи)
4. [Квесты](#квесты)
5. [Организации](#организации)
6. [Уведомления](#уведомления)
7. [Достижения](#достижения)
8. [Поиск и фильтрация](#поиск-и-фильтрация)
9. [Геокодирование](#геокодирование)

---

## Общая информация

### Базовый URL

```
https://api.ecoquest.example.com/v1
```

### Формат ответов

**Успешный ответ:**

```json
{
  "success": true,
  "data": { ... },
  "message": "Опциональное сообщение"
}
```

**Ошибка:**

```json
{
  "success": false,
  "error": {
    "code": "ERROR_CODE",
    "message": "Описание ошибки",
    "details": { ... }
  }
}
```

### Коды статусов HTTP

- `200` - Успешный запрос
- `201` - Ресурс создан
- `400` - Неверный запрос
- `401` - Не авторизован
- `403` - Доступ запрещен
- `404` - Ресурс не найден
- `409` - Конфликт (например, уже создан квест)
- `422` - Ошибка валидации
- `500` - Внутренняя ошибка сервера

### Аутентификация

Большинство эндпоинтов требуют JWT токен в заголовке:

```
Authorization: Bearer <token>
```

---

## Аутентификация и авторизация

### POST /auth/register

Регистрация нового пользователя

**Тело запроса:**

```json
{
	"name": "Иван Иванов",
	"email": "ivan@example.com",
	"password": "securePassword123"
}
```

**Ответ (201):**

```json
{
	"success": true,
	"data": {
		"user": {
			"id": "user-1234567890",
			"name": "Иван Иванов",
			"email": "ivan@example.com",
			"role": [],
			"level": {
				"level": 1,
				"experience": 0,
				"experienceToNext": 100,
				"title": "Новичок"
			},
			"stats": {
				"totalQuests": 0,
				"completedQuests": 0,
				"totalDonations": 0,
				"totalVolunteerHours": 0
			},
			"achievements": [],
			"participatingQuests": [],
			"createdAt": "2024-01-15T10:30:00Z"
		},
		"token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
	}
}
```

**Валидация:**

- `name`: обязательное, минимум 2 символа
- `email`: обязательное, валидный email, уникальный
- `password`: обязательное, минимум 6 символов

---

### POST /auth/login

Вход в систему

**Тело запроса:**

```json
{
	"email": "ivan@example.com",
	"password": "securePassword123"
}
```

**Ответ (200):**

```json
{
  "success": true,
  "data": {
    "user": { ... }, // Полный объект пользователя
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

**Ошибки:**

- `401` - Неверный email или пароль

---

### POST /auth/logout

Выход из системы (инвалидация токена)

**Заголовки:**

```
Authorization: Bearer <token>
```

**Ответ (200):**

```json
{
	"success": true,
	"message": "Вы успешно вышли из системы"
}
```

---

### GET /auth/me

Получить текущего авторизованного пользователя

**Заголовки:**

```
Authorization: Bearer <token>
```

**Ответ (200):**

```json
{
  "success": true,
  "data": {
    "user": { ... } // Полный объект пользователя
  }
}
```

---

## Пользователи

### GET /users/:userId

Получить информацию о пользователе

**Параметры:**

- `userId` (path) - ID пользователя

**Заголовки:**

```
Authorization: Bearer <token> (опционально, для приватных данных)
```

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"user": {
			"id": "user-1234567890",
			"name": "Иван Иванов",
			"email": "ivan@example.com",
			"avatar": "https://example.com/avatars/user-123.jpg",
			"role": ["financial", "volunteer"],
			"level": {
				"level": 5,
				"experience": 250,
				"experienceToNext": 337,
				"title": "Активный"
			},
			"stats": {
				"totalQuests": 3,
				"completedQuests": 1,
				"totalDonations": 15000,
				"totalVolunteerHours": 12
			},
			"achievements": [
				{
					"id": "first_quest",
					"title": "Первый шаг",
					"description": "Присоединились к своему первому квесту",
					"icon": "🎯",
					"rarity": "common",
					"unlockedAt": "2024-01-16T10:00:00Z"
				}
			],
			"participatingQuests": ["quest-1", "quest-2"],
			"createdQuestId": "quest-3",
			"createdOrganizationId": null,
			"createdAt": "2024-01-15T10:30:00Z"
		}
	}
}
```

---

### PATCH /users/:userId

Обновить информацию о пользователе

**Параметры:**

- `userId` (path) - ID пользователя

**Заголовки:**

```
Authorization: Bearer <token>
```

**Тело запроса:**

```json
{
	"name": "Иван Петров",
	"avatar": "https://example.com/new-avatar.jpg"
}
```

**Ответ (200):**

```json
{
  "success": true,
  "data": {
    "user": { ... } // Обновленный объект пользователя
  }
}
```

**Ошибки:**

- `403` - Можно обновлять только свой профиль

---

## Квесты

### GET /quests

Получить список квестов с фильтрацией

**Query параметры:**

- `city` (string, опционально) - Фильтр по городу
- `type` (string, опционально) - Фильтр по типу
- `category` (string, опционально) - Фильтр по категории: `environment`, `animals`, `people`, `education`, `other`
- `status` (string, опционально) - Фильтр по статусу: `active`, `completed`, `archived`
- `assistance` (string[], опционально) - Фильтр по типу помощи (массив ID)
- `search` (string, опционально) - Поиск по названию/описанию
- `page` (number, опционально, default: 1) - Номер страницы
- `limit` (number, опционально, default: 20) - Количество на странице
- `sort` (string, опционально) - Сортировка: `newest`, `oldest`, `progress`, `popular`

**Заголовки:**

```
Authorization: Bearer <token> (опционально, для userParticipation)
```

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"quests": [
			{
				"id": "quest-1",
				"title": "Очистка озера Чистое",
				"city": "Москва",
				"type": "Экология",
				"category": "environment",
				"story": "Озеро Чистое нуждается в нашей помощи...",
				"storyMedia": {
					"image": "https://example.com/lake.jpg"
				},
				"stages": [
					{
						"id": "stage-1",
						"title": "Подготовка оборудования",
						"description": "Закупка необходимого оборудования",
						"status": "completed",
						"progress": 100,
						"requirements": {
							"financial": {
								"collected": 50000,
								"needed": 50000,
								"currency": "RUB"
							}
						}
					},
					{
						"id": "stage-2",
						"title": "Очистка озера",
						"description": "Физическая очистка водоема",
						"status": "in_progress",
						"progress": 60,
						"requirements": {
							"volunteers": {
								"registered": 15,
								"needed": 25
							}
						},
						"deadline": "2024-02-15T00:00:00Z"
					}
				],
				"overallProgress": 80,
				"status": "active",
				"progressColor": "green",
				"updates": [
					{
						"id": "update-1",
						"date": "2024-01-20T10:00:00Z",
						"title": "Начало работ",
						"content": "Мы начали очистку озера...",
						"author": "Куратор проекта",
						"stageId": "stage-2"
					}
				],
				"coordinates": [55.751244, 37.618423],
				"address": "Москва, парк Сокольники",
				"curator": {
					"name": "Иван Петров",
					"phone": "+7 (900) 123-45-67",
					"email": "curator@example.com",
					"organization": "Эко-фонд"
				},
				"socials": [
					{
						"name": "VK",
						"url": "https://vk.com/ecoquest"
					}
				],
				"gallery": ["https://example.com/img1.jpg"],
				"createdAt": "2024-01-10T10:00:00Z",
				"updatedAt": "2024-01-20T10:00:00Z",
				"userParticipation": {
					"userId": "user-123",
					"role": "volunteer",
					"joinedAt": "2024-01-15T10:00:00Z",
					"contributions": [
						{
							"stageId": "stage-2",
							"action": "registered"
						}
					]
				}
			}
		],
		"pagination": {
			"page": 1,
			"limit": 20,
			"total": 45,
			"totalPages": 3
		}
	}
}
```

---

### GET /quests/:questId

Получить детальную информацию о квесте

**Параметры:**

- `questId` (path) - ID квеста

**Заголовки:**

```
Authorization: Bearer <token> (опционально, для userParticipation)
```

**Ответ (200):**

```json
{
  "success": true,
  "data": {
    "quest": { ... } // Полный объект квеста (как в списке)
  }
}
```

---

### POST /quests

Создать новый квест

**Заголовки:**

```
Authorization: Bearer <token>
```

**Тело запроса:**

```json
{
	"title": "Посадка 1000 деревьев",
	"city": "Москва",
	"type": "Экология",
	"category": "environment",
	"story": "Мы хотим посадить 1000 деревьев...",
	"storyMedia": {
		"image": "https://example.com/trees.jpg"
	},
	"stages": [
		{
			"title": "Закупка саженцев",
			"description": "Покупка 1000 саженцев",
			"requirements": {
				"financial": {
					"needed": 200000,
					"currency": "RUB"
				}
			}
		},
		{
			"title": "Посадка деревьев",
			"description": "Физическая посадка",
			"requirements": {
				"volunteers": {
					"needed": 50
				}
			},
			"deadline": "2024-03-01T00:00:00Z"
		}
	],
	"coordinates": [55.751244, 37.618423],
	"address": "Москва, парк Сокольники",
	"curator": {
		"name": "Иван Иванов",
		"phone": "+7 (900) 123-45-67",
		"email": "ivan@example.com",
		"organization": "Эко-фонд"
	},
	"socials": [
		{
			"name": "VK",
			"url": "https://vk.com/ecoquest"
		}
	]
}
```

**Ответ (201):**

```json
{
  "success": true,
  "data": {
    "quest": { ... }, // Созданный квест с ID
    "message": "Квест успешно создан"
  }
}
```

**Ошибки:**

- `409` - Пользователь уже создал квест (один пользователь может создать только один квест)
- `422` - Ошибка валидации

**Валидация:**

- `title`: обязательное, минимум 5 символов
- `city`: обязательное
- `type`: обязательное
- `category`: обязательное, один из: `environment`, `animals`, `people`, `education`, `other`
- `story`: обязательное, минимум 50 символов
- `stages`: обязательное, минимум 1 этап
- `coordinates`: обязательное, массив [lat, lng]
- `address`: обязательное
- `curator.name`: обязательное
- `curator.phone`: обязательное, валидный телефон

---

### PATCH /quests/:questId

Обновить квест

**Параметры:**

- `questId` (path) - ID квеста

**Заголовки:**

```
Authorization: Bearer <token>
```

**Тело запроса:**

```json
{
  "title": "Обновленное название",
  "story": "Обновленная история...",
  "stages": [ ... ] // Полный массив этапов
}
```

**Ответ (200):**

```json
{
  "success": true,
  "data": {
    "quest": { ... } // Обновленный квест
  }
}
```

**Ошибки:**

- `403` - Можно обновлять только свой квест
- `404` - Квест не найден

---

### DELETE /quests/:questId

Удалить квест

**Параметры:**

- `questId` (path) - ID квеста

**Заголовки:**

```
Authorization: Bearer <token>
```

**Ответ (200):**

```json
{
	"success": true,
	"message": "Квест успешно удален"
}
```

**Ошибки:**

- `403` - Можно удалять только свой квест
- `404` - Квест не найден

---

### POST /quests/:questId/participate

Присоединиться к квесту

**Параметры:**

- `questId` (path) - ID квеста

**Заголовки:**

```
Authorization: Bearer <token>
```

**Тело запроса:**

```json
{
	"role": "financial" // "financial" | "volunteer" | "ambassador"
}
```

**Ответ (200):**

```json
{
  "success": true,
  "data": {
    "participation": {
      "userId": "user-123",
      "questId": "quest-1",
      "role": "financial",
      "joinedAt": "2024-01-20T10:00:00Z"
    },
    "user": { ... }, // Обновленный объект пользователя (с новым опытом/достижениями)
    "message": "Вы успешно присоединились к квесту"
  }
}
```

**Ошибки:**

- `409` - Уже участвуете в квесте (можно добавить дополнительную роль)

**Логика:**

- При первом участии разблокируется достижение "Первый шаг" (если это первый квест пользователя)
- Обновляется статистика пользователя (`totalQuests`)
- Если пользователь уже участвует, добавляется новая роль

---

### POST /quests/:questId/contribute

Внести вклад в квест (донат или волонтерство)

**Параметры:**

- `questId` (path) - ID квеста

**Заголовки:**

```
Authorization: Bearer <token>
```

**Тело запроса (финансовый вклад):**

```json
{
	"stageId": "stage-1",
	"role": "financial",
	"amount": 5000
}
```

**Тело запроса (волонтерство):**

```json
{
	"stageId": "stage-2",
	"role": "volunteer",
	"action": "registered"
}
```

**Ответ (200):**

```json
{
  "success": true,
  "data": {
    "contribution": {
      "id": "contrib-123",
      "questId": "quest-1",
      "stageId": "stage-1",
      "userId": "user-123",
      "role": "financial",
      "amount": 5000,
      "contributedAt": "2024-01-20T10:00:00Z",
      "impact": "Внесли 5000 ₽ на этап \"Подготовка оборудования\""
    },
    "user": { ... }, // Обновленный объект пользователя (с новым опытом/уровнем)
    "quest": { ... }, // Обновленный квест (с обновленным прогрессом этапа)
    "achievements": [ ... ], // Массив новых разблокированных достижений (если есть)
    "levelUp": {
      "newLevel": 3,
      "title": "Начинающий",
      "experienceGain": 50
    } // Опционально, если был повышение уровня
  }
}
```

**Логика расчета опыта:**

- Финансовый вклад: `experienceGain = Math.floor(amount / 100)` (1 XP за каждые 100₽)
- Волонтерство: `experienceGain = 10` XP

**Логика повышения уровня:**

- Если `experience >= experienceToNext`:
  - `level += 1`
  - `experience -= experienceToNext` (остаток переносится)
  - `experienceToNext = Math.floor(experienceToNext * 1.5)` (увеличивается на 50%)
  - `title` обновляется на основе нового уровня

**Достижения:**

- При донате >= 50,000₽ разблокируется "Мастер краудфандинга"
- При донате >= 100,000₽ разблокируется "Чемпион донатов"

---

### POST /quests/:questId/stages/:stageId/volunteers

Зарегистрироваться на волонтерское событие

**Параметры:**

- `questId` (path) - ID квеста
- `stageId` (path) - ID этапа

**Заголовки:**

```
Authorization: Bearer <token>
```

**Тело запроса:**

```json
{
	"name": "Иван Иванов",
	"phone": "+7 (900) 123-45-67",
	"email": "ivan@example.com"
}
```

**Ответ (200):**

```json
{
  "success": true,
  "data": {
    "registration": {
      "id": "reg-123",
      "userId": "user-123",
      "questId": "quest-1",
      "stageId": "stage-2",
      "name": "Иван Иванов",
      "phone": "+7 (900) 123-45-67",
      "email": "ivan@example.com",
      "registeredAt": "2024-01-20T10:00:00Z"
    },
    "quest": { ... }, // Обновленный квест (с обновленным количеством зарегистрированных волонтеров)
    "message": "Вы успешно зарегистрированы на событие"
  }
}
```

**Ошибки:**

- `409` - Все места заняты (registered >= needed)
- `404` - Этап не найден или не требует волонтеров

---

### POST /quests/:questId/updates

Добавить обновление к квесту

**Параметры:**

- `questId` (path) - ID квеста

**Заголовки:**

```
Authorization: Bearer <token>
```

**Тело запроса:**

```json
{
	"title": "Начало работ",
	"content": "Мы начали очистку озера...",
	"images": ["https://example.com/img1.jpg"],
	"video": "https://example.com/video.mp4",
	"stageId": "stage-2"
}
```

**Ответ (201):**

```json
{
	"success": true,
	"data": {
		"update": {
			"id": "update-123",
			"questId": "quest-1",
			"date": "2024-01-20T10:00:00Z",
			"title": "Начало работ",
			"content": "Мы начали очистку озера...",
			"images": ["https://example.com/img1.jpg"],
			"video": "https://example.com/video.mp4",
			"author": "Иван Иванов",
			"stageId": "stage-2"
		}
	}
}
```

**Ошибки:**

- `403` - Только куратор квеста может добавлять обновления

---

## Организации

### GET /organizations

Получить список организаций с фильтрацией

**Query параметры:**

- `city` (string, опционально) - Фильтр по городу
- `type` (string, опционально) - Фильтр по типу
- `assistance` (string[], опционально) - Фильтр по типу помощи (массив ID)
- `search` (string, опционально) - Поиск по названию/описанию
- `page` (number, опционально, default: 1) - Номер страницы
- `limit` (number, опционально, default: 20) - Количество на странице

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"organizations": [
			{
				"id": "org-1",
				"name": "Фонд помощи животным",
				"city": "Москва",
				"type": "Благотворительный фонд",
				"assistance": ["volunteers", "donations", "things"],
				"summary": "Помогаем бездомным животным",
				"description": "Подробное описание организации...",
				"mission": "Наша миссия - помочь каждому животному",
				"goals": ["Найти дом для 1000 животных", "Организовать стерилизацию"],
				"needs": ["Корм для животных", "Волонтеры для выгула"],
				"coordinates": [55.751244, 37.618423],
				"address": "Москва, ул. Ленина, д. 1",
				"contacts": {
					"phone": "+7 (900) 123-45-67",
					"email": "info@example.com"
				},
				"website": "https://example.com",
				"socials": [
					{
						"name": "VK",
						"url": "https://vk.com/org"
					}
				],
				"gallery": ["https://example.com/img1.jpg"]
			}
		],
		"pagination": {
			"page": 1,
			"limit": 20,
			"total": 30,
			"totalPages": 2
		}
	}
}
```

---

### GET /organizations/:organizationId

Получить детальную информацию об организации

**Параметры:**

- `organizationId` (path) - ID организации

**Ответ (200):**

```json
{
  "success": true,
  "data": {
    "organization": { ... } // Полный объект организации
  }
}
```

---

### POST /organizations

Создать новую организацию

**Заголовки:**

```
Authorization: Bearer <token>
```

**Тело запроса:**

```json
{
	"name": "Фонд помощи животным",
	"city": "Москва",
	"type": "Благотворительный фонд",
	"assistance": ["volunteers", "donations"],
	"summary": "Краткое описание",
	"description": "Подробное описание организации...",
	"mission": "Наша миссия",
	"goals": ["Цель 1", "Цель 2"],
	"needs": ["Нужно 1", "Нужно 2"],
	"coordinates": [55.751244, 37.618423],
	"address": "Москва, ул. Ленина, д. 1",
	"contacts": {
		"phone": "+7 (900) 123-45-67",
		"email": "info@example.com"
	},
	"website": "https://example.com",
	"socials": [
		{
			"name": "VK",
			"url": "https://vk.com/org"
		}
	]
}
```

**Ответ (201):**

```json
{
  "success": true,
  "data": {
    "organization": { ... }, // Созданная организация с ID
    "message": "Организация успешно создана"
  }
}
```

**Ошибки:**

- `409` - Пользователь уже создал организацию (один пользователь может создать только одну организацию)
- `422` - Ошибка валидации

**Валидация:**

- `name`: обязательное, минимум 3 символа
- `city`: обязательное
- `type`: обязательное
- `assistance`: обязательное, массив, минимум 1 элемент
- `summary`: обязательное, минимум 10 символов
- `description`: обязательное, минимум 50 символов
- `mission`: обязательное, минимум 10 символов
- `coordinates`: обязательное, массив [lat, lng]
- `address`: обязательное
- `contacts.phone`: обязательное, валидный телефон

---

### PATCH /organizations/:organizationId

Обновить организацию

**Параметры:**

- `organizationId` (path) - ID организации

**Заголовки:**

```
Authorization: Bearer <token>
```

**Тело запроса:**

```json
{
  "name": "Обновленное название",
  "description": "Обновленное описание...",
  "goals": [ ... ],
  "needs": [ ... ]
}
```

**Ответ (200):**

```json
{
  "success": true,
  "data": {
    "organization": { ... } // Обновленная организация
  }
}
```

**Ошибки:**

- `403` - Можно обновлять только свою организацию
- `404` - Организация не найдена

---

### DELETE /organizations/:organizationId

Удалить организацию

**Параметры:**

- `organizationId` (path) - ID организации

**Заголовки:**

```
Authorization: Bearer <token>
```

**Ответ (200):**

```json
{
	"success": true,
	"message": "Организация успешно удалена"
}
```

**Ошибки:**

- `403` - Можно удалять только свою организацию
- `404` - Организация не найдена

---

## Уведомления

### GET /notifications

Получить список уведомлений пользователя

**Заголовки:**

```
Authorization: Bearer <token>
```

**Query параметры:**

- `read` (boolean, опционально) - Фильтр по прочитанным/непрочитанным
- `type` (string, опционально) - Фильтр по типу
- `page` (number, опционально, default: 1)
- `limit` (number, опционально, default: 20)

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"notifications": [
			{
				"id": "notif-123",
				"type": "quest_update",
				"title": "Добро пожаловать в квест!",
				"message": "Вы успешно присоединились к квесту \"Очистка озера\"",
				"questId": "quest-1",
				"read": false,
				"createdAt": "2024-01-20T10:00:00Z",
				"actionUrl": "/quests/quest-1",
				"icon": "🎯"
			}
		],
		"unreadCount": 5,
		"pagination": {
			"page": 1,
			"limit": 20,
			"total": 15,
			"totalPages": 1
		}
	}
}
```

**Типы уведомлений:**

- `quest_completed` - Квест завершен
- `stage_completed` - Этап завершен
- `achievement_unlocked` - Достижение разблокировано
- `donation_received` - Получен донат
- `volunteer_registered` - Зарегистрирован волонтер
- `quest_update` - Обновление квеста
- `milestone_reached` - Достигнута веха

---

### PATCH /notifications/:notificationId/read

Отметить уведомление как прочитанное

**Параметры:**

- `notificationId` (path) - ID уведомления

**Заголовки:**

```
Authorization: Bearer <token>
```

**Ответ (200):**

```json
{
  "success": true,
  "data": {
    "notification": { ... } // Обновленное уведомление
  }
}
```

---

### POST /notifications/read-all

Отметить все уведомления как прочитанные

**Заголовки:**

```
Authorization: Bearer <token>
```

**Ответ (200):**

```json
{
	"success": true,
	"message": "Все уведомления отмечены как прочитанные"
}
```

---

### DELETE /notifications/:notificationId

Удалить уведомление

**Параметры:**

- `notificationId` (path) - ID уведомления

**Заголовки:**

```
Authorization: Bearer <token>
```

**Ответ (200):**

```json
{
	"success": true,
	"message": "Уведомление удалено"
}
```

---

### DELETE /notifications

Удалить все уведомления

**Заголовки:**

```
Authorization: Bearer <token>
```

**Ответ (200):**

```json
{
	"success": true,
	"message": "Все уведомления удалены"
}
```

---

## Достижения

### GET /achievements

Получить список всех достижений

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"achievements": [
			{
				"id": "first_quest",
				"title": "Первый шаг",
				"description": "Присоединились к своему первому квесту",
				"icon": "🎯",
				"rarity": "common"
			},
			{
				"id": "donation_champion",
				"title": "Чемпион донатов",
				"description": "Внесли более 100 000 рублей",
				"icon": "💎",
				"rarity": "legendary"
			}
		]
	}
}
```

**Редкости достижений:**

- `common` - Обычное
- `rare` - Редкое
- `epic` - Эпическое
- `legendary` - Легендарное

---

### GET /users/:userId/achievements

Получить достижения пользователя

**Параметры:**

- `userId` (path) - ID пользователя

**Заголовки:**

```
Authorization: Bearer <token> (опционально)
```

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"achievements": [
			{
				"id": "first_quest",
				"title": "Первый шаг",
				"description": "Присоединились к своему первому квесту",
				"icon": "🎯",
				"rarity": "common",
				"unlockedAt": "2024-01-16T10:00:00Z"
			}
		],
		"unlockedCount": 1,
		"totalCount": 10
	}
}
```

---

## Поиск и фильтрация

### GET /search

Универсальный поиск по квестам и организациям

**Query параметры:**

- `q` (string, обязательное) - Поисковый запрос
- `type` (string, опционально) - Тип результата: `quests`, `organizations`, `all` (default: `all`)
- `city` (string, опционально) - Фильтр по городу
- `limit` (number, опционально, default: 10) - Количество результатов

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"results": {
			"quests": [
				{
					"id": "quest-1",
					"title": "Очистка озера",
					"city": "Москва",
					"type": "Экология",
					"coordinates": [55.751244, 37.618423],
					"address": "Москва, парк Сокольники"
				}
			],
			"organizations": [
				{
					"id": "org-1",
					"name": "Эко-фонд",
					"city": "Москва",
					"type": "Благотворительный фонд",
					"coordinates": [55.751244, 37.618423],
					"address": "Москва, ул. Ленина, д. 1"
				}
			]
		}
	}
}
```

---

## Геокодирование

### POST /geocode

Преобразовать адрес в координаты

**Тело запроса:**

```json
{
	"address": "Москва, Красная площадь, 1"
}
```

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"results": [
			{
				"lat": 55.75393,
				"lon": 37.620792,
				"display_name": "Москва, Красная площадь, 1",
				"place_id": 123456
			}
		]
	}
}
```

**Примечание:** Можно использовать внешний сервис (например, Nominatim от OpenStreetMap) или собственную реализацию.

---

## Дополнительные эндпоинты

### GET /cities

Получить список всех городов (для фильтров)

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"cities": ["Москва", "Санкт-Петербург", "Новосибирск"]
	}
}
```

---

### GET /quest-types

Получить список всех типов квестов

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"types": ["Экология", "Животные", "Люди", "Образование"]
	}
}
```

---

### GET /organization-types

Получить список всех типов организаций

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"types": ["Благотворительный фонд", "Волонтерская организация", "НКО"]
	}
}
```

---

### GET /assistance-types

Получить список всех типов помощи

**Ответ (200):**

```json
{
	"success": true,
	"data": {
		"types": [
			{
				"id": "volunteers",
				"label": "Требуются волонтеры"
			},
			{
				"id": "donations",
				"label": "Нужны финансовые пожертвования"
			},
			{
				"id": "things",
				"label": "Принимают вещи"
			},
			{
				"id": "mentors",
				"label": "Требуются наставники"
			},
			{
				"id": "blood",
				"label": "Нужны доноры"
			},
			{
				"id": "experts",
				"label": "Требуются эксперты"
			}
		]
	}
}
```

---

## Важные замечания

### Ограничения

1. **Один пользователь = один квест**: Пользователь может создать только один квест. При попытке создать второй возвращается ошибка `409`.

2. **Один пользователь = одна организация**: Пользователь может создать только одну организацию. При попытке создать вторую возвращается ошибка `409`.

3. **Участие в квесте**: Пользователь может участвовать в нескольких квестах одновременно, но в одном квесте может иметь несколько ролей (`financial`, `volunteer`, `ambassador`).

### Расчет опыта и уровня

- **Финансовый вклад**: `experienceGain = Math.floor(amount / 100)` (1 XP за каждые 100₽)
- **Волонтерство**: `experienceGain = 10` XP
- **Повышение уровня**:
  - Если `experience >= experienceToNext`:
    - `level += 1`
    - `experience -= experienceToNext`
    - `experienceToNext = Math.floor(experienceToNext * 1.5)`
    - `title` обновляется на основе уровня (см. `src/utils/level.ts`)

### Названия уровней

- 1: "Новичок"
- 2: "Ученик"
- 3-4: "Начинающий"
- 5-9: "Активный"
- 10-14: "Продвинутый"
- 15-19: "Опытный"
- 20-29: "Профессионал"
- 30-39: "Эксперт"
- 40-49: "Мастер"
- 50+: "Легенда"

### Достижения

Достижения разблокируются автоматически при выполнении условий:

- `first_quest` - При присоединении к первому квесту
- `donation_champion` - При донате >= 100,000₽
- `crowdfunding_master` - При донате >= 50,000₽
- `lake_saver` - При участии в квесте "ozero-chistoe"
- `tree_planter` - При участии в квесте "les-1000-derev"
- `wildlife_protector` - При участии в квесте "volk-berkut"

### Прогресс квеста

Общий прогресс квеста (`overallProgress`) рассчитывается как среднее арифметическое прогресса всех этапов:

```javascript
overallProgress = Math.round(
	stages.reduce((sum, stage) => sum + stage.progress, 0) / stages.length
)
```

### Цвет прогресса

- `red`: 0-20%
- `orange`: 21-40%
- `yellow`: 41-60%
- `green`: 61-99%
- `victory`: 100%

---

## Примеры использования

### Полный цикл: создание квеста и участие

1. **Регистрация пользователя**

```bash
POST /auth/register
```

2. **Создание квеста**

```bash
POST /quests
Authorization: Bearer <token>
```

3. **Присоединение к квесту**

```bash
POST /quests/:questId/participate
Authorization: Bearer <token>
```

4. **Внесение финансового вклада**

```bash
POST /quests/:questId/contribute
Authorization: Bearer <token>
{
  "stageId": "stage-1",
  "role": "financial",
  "amount": 5000
}
```

5. **Регистрация на волонтерское событие**

```bash
POST /quests/:questId/stages/:stageId/volunteers
Authorization: Bearer <token>
{
  "name": "Иван Иванов",
  "phone": "+7 (900) 123-45-67",
  "email": "ivan@example.com"
}
```

---

## Контакты

Если у вас есть вопросы по API, обращайтесь к команде разработки.
